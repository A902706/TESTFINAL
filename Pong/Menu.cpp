#include "Menu.hpp"



void Menu::spawnMenu(sf::RenderWindow& window)
{
	//sf::Font font;
	//font.loadFromFile("font.TTF");

	////Welcome screen
	//sf::Text gameTitle("Welcome to Space Invaders (kinda)", font, 150);
	//gameTitle.setFillColor(sf::Color::Black);
	//sf::FloatRect bounds2(gameTitle.getLocalBounds());
	//gameTitle.setPosition(window.getSize().x / 2 - (bounds2.left + bounds2.width / 2), window.getSize().y / 2 - (bounds2.top + bounds2.height / 2));

	//window.draw(gameTitle);
	//window.display();


	
 
	std::cout << "START GAME" << std::endl;
	std::cout << "INSTRUCTION" << std::endl;
	std::cout << "EXIT GAME" << std::endl;

	
}
